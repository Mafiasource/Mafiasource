{% extends "/app/Resources/Views/base.admin.twig" %}

{% block content %}
<div class="row">
	<div class="col-md-12">
        <div class="content-box-header">
			<div class="panel-title">Forum statussen beheren</div>
			
			<div class="panel-options">
				<a href="#" data-rel="collapse"><i class="glyphicon glyphicon-refresh"></i></a>
			</div>
		</div>
		<div class="content-box-large box-with-header">
            <p>Forum statussen kunnen in-game of in deze admincp worden toegepast op topics.</p>
            {% set tableRows = forumStats %}
            {% set dataTable = 'forum_status' %}
            {% set startvar = pagination.from %}
            {# {% set skipFields = ['article_nl','article_en'] %} #}
            {% set onlyFields = ['id','status_nl'] %}
            {% include "/app/Resources/Views/recordstable.twig" %}
            
            <a href="#" class="btn btn-default editOrNew">Nieuwe status aanmaken</a>
        </div>
    </div>
</div>
{% endblock content %}

{% block javascripts %}
    {% include "src/Views/admin/.recordstable.js.twig" %}
{% endblock javascripts %}
