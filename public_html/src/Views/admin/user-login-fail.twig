{% extends "/app/Resources/Views/base.admin.twig" %}

{% block content %}
<div class="row">
	<div class="col-md-12">
        <div class="content-box-header">
			<div class="panel-title">Mislukte Inlogpogingen</div>
            
			<div class="panel-options">
				<a href="#" data-rel="collapse"><i class="glyphicon glyphicon-refresh"></i></a>
			</div>
		</div>
		<div class="content-box-large box-with-header">
            
            {% set tableRows = login %}
            {% set dataTable = 'login_fail' %}
            {% set startvar = pagination.from %}
            {# {% set skipFields = [''] %} #}
            {% set onlyFields = ['username', 'date', 'type', 'cookieLogin'] %}
            {% include "/app/Resources/Views/recordstable.twig" %}
            <br class="clear" />
        </div>
    </div>
</div>
{% endblock content %}

{% block javascripts %}
    {% include "src/Views/admin/.recordstable.js.twig" %}
{% endblock javascripts %}
