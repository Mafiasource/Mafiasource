{% extends "/app/Resources/Views/base.admin.twig" %}

{% block content %}
<div class="row">
	<div class="col-md-12">
        <div class="content-box-header">
			<div class="panel-title">Hall of fame spelrondes beheren</div>
            
			<div class="panel-options">
				<a href="#" data-rel="collapse"><i class="glyphicon glyphicon-refresh"></i></a>
			</div>
		</div>
		<div class="content-box-large box-with-header">
            <p>Voeg hier verlopen spelrondes toe die zonder dit Admincp werden gereset.</p>
            <p>Om een reset uit te voeren incl. de aanmaak van de Hall of fame spelronde <a href="{{ routing.getRouteByRouteName('admin-reset') }}">klik je hier</a>.</p>
            
            {% set tableRows = round %}
            {% set dataTable = 'round' %}
            {% set startvar = pagination.from %}
            {# {% set skipFields = [''] %} #}
            {% set onlyFields = ['round', 'startDate', 'endDate'] %}
            {% include "/app/Resources/Views/recordstable.twig" %}
            
            <a href="#" class="btn btn-default editOrNew">Nieuwe spelronde aanmaken</a>
        </div>
    </div>
</div>
{% endblock content %}

{% block javascripts %}
    {% include "src/Views/admin/.recordstable.js.twig" %}
{% endblock javascripts %}
