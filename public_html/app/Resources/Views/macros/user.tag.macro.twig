{# Beware! user.tag.macro only to be used in-game!
   User tag is authenticated with a fake userData.getId
   Authentication now used to remove profile URL on the outgame pages for non authenticated users
   Auth version should be used for any (future) outgame page including atleast one user tag
   Auth usage: see /src/Views/forum.twig (authUserTagManager) #}

{% macro write(name, id, userID, username, donatorID, userClass, avatar='Avatarless', imgMobileHide='False', largeImg='False') %}
    {# setting fake userData with ID of admin shouldn't hurt inside macro #}
    {% include "/app/Resources/Views/macros/.user.tag.twig" with {userData: {getId: 1}} %}
{% endmacro write %}
