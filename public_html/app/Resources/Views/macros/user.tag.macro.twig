{# Beware! user.tag.macro only to be used in-game (always autheticated)
   User tag macro is 'authenticated' with a fake userData.getId = 1 (always authenticated)
   Authentication is only used to remove profile URL on outgame pages for non-authenticated users #}

{% macro write(name, id, userID, username, donatorID, userClass, avatar='Avatarless', imgMobileHide='False', largeImg='False') %}
    {# setting fake userData with ID of admin shouldn't hurt inside macro #}
    {% include "/app/Resources/Views/macros/.user.tag.twig" with {userData: {getId: 1}} %}
{% endmacro write %}
